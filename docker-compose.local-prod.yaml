services:
  caddy:
    volumes:
      - ./Caddyfile.local:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
      
  frontend:
    build:
      context: .
      dockerfile: ./client/Dockerfile

  backend:
    build:
      context: .
      dockerfile: ./server/Dockerfile